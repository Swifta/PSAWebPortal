package com.swifta.mats.web.utils;


public class FeesTester {
	// public static void main(String args[]) {
	// int commissionCount = 4, transactionTypeId = 5, feeCount = 4;
	// ServiceCommission[] serviceCommission = new
	// ServiceCommission[commissionCount];
	// ServiceCommission newServiceCommission = new ServiceCommission();
	// BigDecimal[] commissionFeeArray = { new BigDecimal(40),
	// new BigDecimal(70), new BigDecimal(100), new BigDecimal(120) };
	// BigDecimal[] minAmtArray = { new BigDecimal(500.0),
	// new BigDecimal(2000), new BigDecimal(10000),
	// new BigDecimal(40000) };
	//
	// BigDecimal[] maxAmtArray = { new BigDecimal(1999.0),
	// new BigDecimal(9999), new BigDecimal(39999),
	// new BigDecimal(100000) };
	//
	// for (int i = 0; i < commissionCount; i++) {
	// newServiceCommission = new ServiceCommission();
	// newServiceCommission.setCommissionfee(commissionFeeArray[i]);
	// newServiceCommission
	// .setCommissionfeetype(ServiceCommissionmatrix.FIXED);
	// newServiceCommission.setMaximumamount(maxAmtArray[i]);
	// newServiceCommission.setMinimumamount(minAmtArray[i]);
	// newServiceCommission
	// .setServicecommissioncondition(ServiceCommissionConditionTypes.FEE);
	// newServiceCommission
	// .setServicecommissionmodeltype(ServiceCommissionModelTypes.NOTAPPLICABLE);
	// // either cashin cashout ID
	// newServiceCommission.setTransactiontypeid(transactionTypeId);
	// serviceCommission[i] = newServiceCommission;
	// }
	// ServiceFees[] serviceFeesArra = new ServiceFees[feeCount];
	// BigDecimal[] serviceFeeArray = { new BigDecimal(0), new BigDecimal(0),
	// new BigDecimal(0), new BigDecimal(0) };
	// for (int i = 0; i < feeCount; i++) {
	// ServiceFees serviceFees = new ServiceFees();
	// serviceFees.setMinimumamount(minAmtArray[i]);
	// serviceFees.setMaximumamount(maxAmtArray[i]);
	// serviceFees.setServicefee(serviceFeeArray[i]);
	// if (i == 4)
	// serviceFees.setServicefeetype(ServiceFeematrix.PERCENT);
	// else
	// serviceFees.setServicefeetype(ServiceFeematrix.FIXED);
	// serviceFees.setTransactiontypeid(transactionTypeId);
	//
	// serviceFeesArra[i] = serviceFees;
	// }
	//
	// CommissionService commissionService = new CommissionService();
	// System.out.println("The final Result after the integration::::::"
	// + commissionService.setFeesAndCommission("4", 5,
	// serviceCommission, serviceFeesArra));
	// }

}
